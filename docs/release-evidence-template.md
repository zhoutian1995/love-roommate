# Love Roommate 发布证据模板

> 只填写脱敏后的相对路径、哈希和测试结论。不得写入源照片、用户名、主机名或本机绝对路径。任何必需项缺失都不能宣布发布完成。

## GitHub 精确版本

- 仓库：`zhoutian1995/love-roommate`
- 发布标签：`<填写稳定标签>`
- commit SHA：`<填写 40 位 SHA>`
- tree SHA：`<填写 tree SHA>`
- tracked 文件数：`<填写数字>`
- clean clone 状态：`<clean / failed>`
- README 普通安装命令已替换 `<stable-tag>` 为真实稳定标签：`<是 / 否>`
- README 审计示例从本发布证据复制精确 SHA，不把当前 commit SHA 硬编码进同一 commit：`<是 / 否>`
- 根目录 `LICENSE`、`THIRD_PARTY_NOTICES.md` 与冻结依赖版本一致：`<是 / 否>`

## CI 终态

| 门禁 | 运行链接或编号 | 终态 |
| --- | --- | --- |
| Windows release-check | `<填写>` | `<success / failed>` |
| macOS release-check | `<填写>` | `<success / failed>` |
| official-skill-validator | `<填写>` | `<success / failed>` |

## 官方安装器

- 固定 `--method download --ref <精确 SHA>`：`<通过 / 失败>`
- 隔离安装目录包含 `.git`：`<否 / 是>`
- missing / extra / SHA mismatch：`<0 / 0 / 0>`
- 安装文件数：`<填写数字>`

## 陌生用户 E2E

- 输入类型：`<授权照片 / 虚构人物>`
- 人数及本人位置：`<1～8 人；本人编号>`
- 与开发示范人物是否独立：`<是 / 否>`
- 新建项目、身份母版、全部动作、record/process：`<通过 / 失败>`
- 透明恢复路径：`<自动通过 / 本地修正通过 / 失败关闭>`
- self-check 分数与状态：`<填写>`
- contact sheet 与 identity review：`<通过 / 失败>`
- 本人不在照片时的流程：`<通过 / 失败>`
- 提问仅包含授权、本人、中文模式和最终确认：`<通过 / 失败>`
- 未询问恶搞排除、领头人、跟随者或接力顺序：`<通过 / 失败>`

## 随机 1～8 人契约覆盖

自动测试必须逐一覆盖 1、2、3、4、5、6、7、8 人，不得用“五人通过”代替随机合照支持。本人位置必须覆盖每个可选位置，并单独覆盖“本人不在照片”。

| 人数 | 动态人物 ID / 窗口数 | 本人在每个位置 | 本人不在 | 结论 |
| ---: | --- | --- | --- | --- |
| 1 | `<通过 / 失败>` | `<通过 / 失败>` | `<通过 / 失败>` | `<填写>` |
| 2 | `<通过 / 失败>` | `<通过 / 失败>` | `<通过 / 失败>` | `<填写>` |
| 3 | `<通过 / 失败>` | `<通过 / 失败>` | `<通过 / 失败>` | `<填写>` |
| 4 | `<通过 / 失败>` | `<通过 / 失败>` | `<通过 / 失败>` | `<填写>` |
| 5 | `<通过 / 失败>` | `<通过 / 失败>` | `<通过 / 失败>` | `<填写>` |
| 6 | `<通过 / 失败>` | `<通过 / 失败>` | `<通过 / 失败>` | `<填写>` |
| 7 | `<通过 / 失败>` | `<通过 / 失败>` | `<通过 / 失败>` | `<填写>` |
| 8 | `<通过 / 失败>` | `<通过 / 失败>` | `<通过 / 失败>` | `<填写>` |

## 视觉验收

- 身份、完整身体、透明边缘：`<通过 / 失败>`
- 普通桌宠 / 集体跪喊 / 屎追逐 / 全部都要：`<通过 / 失败>`
- dad / grandpa 慢移、排队、跪拜、动作帧 0→1→2：`<通过 / 失败>`
- 有本人：本人站立不跪、其他全员跪喊；本人持续拉、其他全员追吃：`<通过 / 失败>`
- 没有指定本人：全员跪喊；鼠标控制点击穿透的屎、全员人形蜈蚣追随：`<通过 / 失败>`
- 八人窄屏居中多排、顺序稳定、无重叠、无越界：`<通过 / 失败>`
- 证据索引相对路径：`<填写>`

## 搞笑程度人工门禁

每个实际启用的特殊恶搞都要单独评分、绑定当前场景 PNG 证据、写扣分原因、优化动作并在重新截图后复评。任一特殊恶搞总分低于 90 必须整体失败，不能用其它恶搞高分或技术、性能、隐私门禁代替。六项必须为范围内整数，总分严格等于六项之和；对象、布尔值、空白和零宽字符不算有效文字。

- self-check review schema：`<必须为 3>`
- 评分契约 fingerprint：`<填写 64 位 SHA-256>`
- 当前启用 prankId 精确集合：`<dad-shout / grandpa-shout / poop-chase 或 cursor-centipede>`
- 旧审核 fingerprint 已失效：`<是 / 否>`

| prankId | 当前 evidenceRefs | 角色关系 25 | 荒诞程度 20 | 动作与节奏 20 | 队形表现 15 | 屎的可读性 10 | 回看欲 10 | 总分 | 扣分原因 / 优化 / 复评 |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| `dad-shout` | `<preview/scenarios/dad-shout/*.png>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` |
| `grandpa-shout` | `<preview/scenarios/grandpa-shout/*.png>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` |
| `poop-chase` 或 `cursor-centipede` | `<对应 preview/scenarios/.../*.png>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` | `<填写>` |

## Windows 性能验收

五窗口回归和最终八窗口候选必须来自同一发布版本，均须直接审计真实 packaged `.exe`，并使用完全相同的发布阈值。任一项缺失或失败都不能发布。

- `validate_performance_release.mjs` 5+8 聚合校验：`<通过 / 失败>`
- 两份报告候选代码 fingerprint 完全一致：`<是 / 否；填写 fingerprint>`
- 所有阶段逐秒 pet window id / visible / destroyed 证据完整：`<是 / 否>`

### 五窗口 packaged 回归

- 配置人数 / `expectedWindowCount` / `windowCount`：`<5 / 5 / 5>`
- 真实 Windows packaged full audit：`<通过 / 失败>`
- fps / frame p95 / long pause / CPU：`<填写>`
- private bytes 峰值 / 10 分钟增长：`<填写>`
- Pause CPU 比例 / ticker 比例 / 启动时间：`<填写>`
- `.exe` SHA-256：`<填写>`
- 项目 runtime fingerprint / 候选代码 fingerprint：`<填写>`
- 脱敏报告相对路径 / SHA-256：`<填写>`

### 最终八窗口 packaged 候选

- 配置人数 / `expectedWindowCount` / `windowCount`：`<8 / 8 / 8>`
- 真实 Windows packaged full audit：`<通过 / 失败>`
- 与五窗口阈值完全一致：`<是 / 否>`
- fps / frame p95 / long pause / CPU：`<填写>`
- private bytes 峰值 / 10 分钟增长：`<填写>`
- Pause CPU 比例 / ticker 比例 / 启动时间：`<填写>`
- 八窗口全程存在且未以删人、删动作或取消慢移排队换取通过：`<是 / 否>`
- `.exe` SHA-256：`<填写>`
- 项目 runtime fingerprint / 候选代码 fingerprint：`<填写>`
- 脱敏报告相对路径 / SHA-256：`<填写>`

## Windows 打包

- portable 目录 smoke：`<通过 / 失败>`
- `.exe` 直接启动：`<通过 / 失败>`
- `.exe` SHA-256：`<填写>`
- 文件清单 SHA-256：`<填写>`
- 托盘暂停/退出、拖拽、右键、透明像素点击穿透：`<通过 / 失败>`

## Apple Silicon macOS 验收

- 在 Apple Silicon Mac 上从同一精确 commit 构建：`<通过 / 失败>`
- 本机 ad-hoc 签名且通过 `codesign --verify --deep --strict` 的 arm64 `.app` 启动 smoke：`<通过 / 失败>`
- 透明窗口、托盘、拖拽、右键与透明像素点击穿透：`<通过 / 失败>`
- `.app` 文件清单 SHA-256：`<填写>`
- 框架相对符号链接保持正确：`<通过 / 失败>`

## 隐私审计

- Skill 仓库审计：`<通过 / 失败>`
- 完整 output root 审计：`<通过 / 失败>`
- 原图精确副本 / 符号链接：`<0 / 0>`
- 私有绝对路径、用户名、主机名：`<0>`
- 审计报告相对路径与 SHA-256：`<填写>`
- 发布仓库不含 `preview/`、`release/`、`node_modules/`、依赖缓存或任何栅格人物图：`<是 / 否>`
- 公开证据只含虚构人物的脱敏 JSON，不含图片、用户名、主机名和绝对路径：`<是 / 否>`

## 精确版本回装复验

- 从 GitHub 精确 commit 的干净 clone 复跑 unified release check：`<通过 / 失败>`
- 官方安装器隔离安装后复跑 Skill validator：`<通过 / 失败>`
- 隔离安装与 Git tree 逐文件 SHA-256 一致：`<通过 / 失败>`
- Live Skill 来自该 exact ref，而非手工零散覆盖：`<是 / 否>`

## 最终独立复核

- 复核者未参与实现：`<是 / 否>`
- 逐项直接证据完整：`<是 / 否>`
- 未解决问题：`<无；否则逐项填写>`
- 最终结论：`<可以发布 / 不可发布>`
